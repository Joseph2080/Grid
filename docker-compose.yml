services:
  db:
    image: postgres:17
    container_name: vektrlabs-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: vektrlabs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: